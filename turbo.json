{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "@opencourser/interfaces#build-types": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false,
      "dependsOn": ["@opencourser/interfaces#build-types"]
    }
  }
}
